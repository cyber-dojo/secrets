secret-name: WEB_SECRET_KEY_BASE
secret-expire: 2027-04-13
is-secret: true

secret-updated: 2026-04-13
secret-updated-by: JonJagger
secret-type: gh-repo

secret-usage:
Signs the Rack session cookie in the Sinatra web app when deploying
to production. Must be stable across container restarts and shared by
all ECS instances so that CSRF tokens survive requests being routed
to different instances. Use a different value from the beta secret in
cyber-dojo/web. Terraform validation requires at least 128 characters
(SecureRandom.hex(64) output).

Update-instructions:
Generate a new value: ruby -e "require 'securerandom'; puts SecureRandom.hex(64)"

Go to https://github.com/cyber-dojo/aws-prod-co-promotion/settings/secrets/actions/WEB_SECRET_KEY_BASE
Click [enter a new value], set the new value, click [Save changes].
